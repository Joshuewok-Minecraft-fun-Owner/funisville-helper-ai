{
  "name": "stream-ai-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply stream-agent-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply stream-agent-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20250101.0"
  }
}
